export function Toast({ message, tone = "error", clear }: { message: string; tone?: "error" | "success"; clear: () => void }) { return <div className="toast toast-end z-50"><div role="status" className={`alert ${tone === "error" ? "alert-error" : "alert-success"}`}><span>{message}</span><button className="btn btn-ghost min-h-11 min-w-11 p-0" onClick={clear} aria-label="Dismiss notification">✕</button></div></div>; }
