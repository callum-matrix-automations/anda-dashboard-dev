export function LoadingState({ label = "Loading records" }: { label?: string }) { return <div aria-label={label} className="grid gap-3"><div className="skeleton h-16 w-full"/><div className="skeleton h-52 w-full"/></div>; }
export function ErrorState({ message, retry }: { message: string; retry?: () => void }) { return <div role="alert" className="alert alert-error"><span>{message}</span>{retry && <button className="btn btn-sm min-h-11" onClick={retry}>Try again</button>}</div>; }
export function EmptyState({ title, body }: { title: string; body: string }) { return <div className="hero min-h-52 rounded-box border border-base-300 bg-base-200"><div className="hero-content text-center"><div><h2 className="text-lg font-semibold">{title}</h2><p className="mt-2 text-sm opacity-65">{body}</p></div></div></div>; }
