# Copy this file to .env.local and replace every placeholder before running the app.
# Do not commit .env.local or put real credentials in this example file.

# Local Supabase defaults. Replace these values in the hosting provider for
# staging and production. Never expose the secret key to browser code.
SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-local-publishable-key
SUPABASE_SECRET_KEY=replace-with-local-secret-key

# Temporary local-only identity for the meeting and archive controller APIs.
# Use the UUID of an ACTIVE MEMBER profile seeded in local Supabase. This value
# is ignored when NODE_ENV=production; Microsoft OAuth will replace this resolver.
ANDA_DEV_ACTOR_PROFILE_ID=replace-with-an-active-local-profile-uuid

# Receiver used by the Read AI-format transcript sender script. HTTP is valid for localhost.
MOCK_TRANSCRIPT_WEBHOOK_URL=http://localhost:3000/api/webhooks/transcripts
# Server-only Base64 signing key generated when the Read AI webhook is created.
READ_AI_WEBHOOK_SIGNING_KEY=replace-with-a-read-ai-base64-signing-key

# Server-only OpenAI configuration. Never expose this key through NEXT_PUBLIC_*.
OPENAI_API_KEY=replace-with-an-openai-project-key
OPENAI_MODEL=gpt-4.1

# Server-only bearer token for POST /api/internal/meetings/{meetingId}/analysis.
# Send this value in the Authorization header as: Bearer <secret>
INTERNAL_ANALYSIS_SECRET=replace-with-a-long-random-internal-secret

# Server-only Firma signing configuration. The test signer is used until the
# active Treasurer is supplied by the accounts workflow.
FIRMA_API_KEY=replace-with-a-firma-workspace-api-key
FIRMA_WORKSPACE_ID=replace-with-the-firma-workspace-id
FIRMA_WEBHOOK_SECRET=replace-with-the-matching-workspace-webhook-signing-secret
# Set during secret rotation while Firma sends X-Firma-Signature-Old.
FIRMA_WEBHOOK_SECRET_PREVIOUS=
SIGNING_TEST_SIGNER_FIRST_NAME=Test
SIGNING_TEST_SIGNER_LAST_NAME=Treasurer
SIGNING_TEST_SIGNER_EMAIL=treasurer@example.test

# Optional local live-callback test settings.
FIRMA_CLOUDFLARED_PATH=C:\Program Files (x86)\cloudflared\cloudflared.exe
FIRMA_CALLBACK_LOCAL_PORT=3010
FIRMA_CALLBACK_WAIT_MS=900000

# Server-only protection for the internal archive recovery endpoint. Public
# archive reads use the same server actor resolver as the meeting controller APIs.
ARCHIVE_API_SECRET=replace-with-a-long-random-archive-secret
ARCHIVE_SIGNED_URL_SECONDS=300

# Server-only scheduler protection and provider-independent recovery settings.
# Configure the production scheduler to POST /api/internal/operations/recover
# at the interval below; the application does not depend on a scheduler vendor.
OPERATIONS_SCHEDULER_SECRET=replace-with-a-long-random-scheduler-secret
OPERATIONS_RECOVERY_INTERVAL_MINUTES=5
OPERATIONS_SIGNING_STALE_MINUTES=10
OPERATIONS_SIGNING_BATCH_LIMIT=25
OPERATIONS_SIGNING_MAX_ATTEMPTS=5
OPERATIONS_ARCHIVE_BATCH_LIMIT=25
OPERATIONS_ARCHIVE_MAX_ATTEMPTS=3
OPERATIONS_ALERT_BATCH_LIMIT=25
OPERATIONS_ALERT_MAX_ATTEMPTS=3
OPERATIONS_ALERT_RETRY_DELAY_SECONDS=30

# Optional server-only Telegram operational alert destination. Leave both blank
# until the bot and destination chat have been created.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
